version: "3"
services:

#  nginx:
#    image: nginx:1.15-alpine
#    ports:
#      - "80:80"
#      - "443:443"
#    volumes:
#      - ./conf/nginx:/etc/nginx/conf.d

  # General server acting as reverse proxy to the api
  nginx:
    image: owasp/modsecurity
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx:/etc/nginx/conf.d

  # Client for generating CA certs
  certbot:
      image: certbot/certbot

  # Api infrastructure as backend for clients (i.e. pluto)
  api:
    container_name: deepmarket_api

    build: ./api
    image: "api:latest"

    restart: always

    ports:
      - "8080:8080"

    links:
      - generate_prices

    depends_on:
      - mongo

    # volumes:
    #   - .:/api
    #   - /api/node_modules

  # Price generation microservice
  generate_prices:
    container_name: deepmarket_gen_prices

    build: ./core/generate_prices
    image: "generate_prices:latest"

    # restart: always
    environment:
      - API_PRICING_ENDPOINT=http://localhost:8080/api/v1/pricing

        # depends_on:
        # - api

  # Mongo db instance as backend for api
  mongo:
    container_name: mongo
    image: mongo

    volumes:
      - /data/db:/data/db

    ports:
      - "27017:27017"
